# Persistent exceptions for ci/check_no_secrets.py.
#
# Format:  <repo-relative path>::<RULE_ID>     (or <path>::* for every rule)
# Prefer an inline `# ci-allow-secret` comment on the single offending line;
# use this file only when the pattern repeats across a whole file.
#
# Empty today: neither repo trips a blocking rule.
