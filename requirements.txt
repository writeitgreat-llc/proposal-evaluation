Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
openai==1.68.2
httpx==0.28.1
xhtml2pdf==0.2.16
python-docx==1.1.0
pypdf==6.14.2
Werkzeug==3.0.1
itsdangerous==2.2.0
gunicorn==21.2.0
psycopg2-binary==2.9.9
python-dotenv==1.0.0
pyotp==2.9.0
qrcode==7.4.2
Pillow==12.3.0
requests==2.32.5
# Error tracking. Pinned exactly, and 2.66.1 specifically: pip-audit reports no
# known vulnerability against it (2026-08-03), so it adds nothing to
# ci/vuln_baseline.txt. It is inert without SENTRY_DSN — see observability.py —
# so installing it here does not by itself start sending anything anywhere.
sentry-sdk==2.66.1
