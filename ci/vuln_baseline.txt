# proposal-evaluation -- accepted dependency advisories
#
# Format:  <ADVISORY-ID>   # why it is accepted ... expires: YYYY-MM-DD
#
# Only findings at or above the gate threshold (HIGH) need to be listed.
# LOW/MODERATE findings are printed every run but do not block.
#
# THIS FILE IS DELIBERATELY EMPTY (2026-08-05).
#
# It held 16 entries covering three pins. All three are now bumped, so every
# entry is gone -- fixed, not muted:
#
#     Pillow    10.2.0  -> 12.3.0   (2026-07-21, commit ac5470b)   7 entries
#     gunicorn  21.2.0  -> 23.0.0   (2026-08-05)                   4 entries
#     Werkzeug  3.0.1   -> 3.1.8    (2026-08-05)                   4 entries
#
# Those 16 lines were 8 distinct advisories: OSV publishes each one under both
# a GHSA and a PYSEC id, and both ids were listed so that whichever one
# pip-audit happened to report would match. Worth knowing before anyone counts
# lines here and reports it as a vulnerability total.
#
# `pip-audit -r requirements.txt` now returns FOUR findings, all below the HIGH
# gate, none baselined and none silenced -- they print on every run by design:
#
#     MODERATE  python-dotenv 1.0.0   PYSEC-2026-2270  fix: 1.2.2
#     MODERATE  requests      2.32.5  PYSEC-2026-2275  fix: 2.33.0
#     MODERATE  xhtml2pdf     0.2.16  PYSEC-2026-2056  NO FIX RELEASED
#     LOW       Flask         3.0.0   PYSEC-2026-2151  fix: 3.1.3
#
# The file is kept rather than deleted so the path CI passes to --baseline
# still resolves and the format above survives for whoever needs it next. An
# empty baseline and a missing one behave identically to ci/pip_audit_gate.py;
# an empty one records that the emptiness is deliberate.
#
# ---------------------------------------------------------------------------
# BEFORE YOU ADD AN ENTRY HERE
#
# A baseline entry is an accepted risk with a date on it, not a mute button.
# Reach for it only when there is genuinely no fixed version to move to. If a
# fix exists, bump the pin -- that is what cleared all 16 of these, and in
# every case the bump was smaller than the argument for baselining it.
#
# An `expires:` date is REQUIRED. As of 2026-08-05 .github/workflows/security.yml
# passes --enforce-expiry, so a past-dated entry FAILS the build instead of
# printing a STALE line nobody reads. That flag is why this file sat full for
# weeks after the fixes were available: without it the dates were decorative,
# and nothing anywhere would ever have forced these three bumps.
